import CartContent from "@/components/cart/CartContent";

export const metadata = {
    title: "Shopping Cart | Sprazo",
    description: "Manage your shopping cart at Sprazo.",
};

export default function CartPage() {
    return <CartContent />;
}
